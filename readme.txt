Install the required packages. To do so, run the following command:
pip install -r requirements.txt
This is a small python script to track the current value of USDC price on a coingecko api to check if it depegs.
You can change the ranges and alerts to suit your needs.